# Rules

- 每次对话结束时，先运行 `pnpm run lint`、`tsc --noEmit`、`pnpm run format`，修复所有问题后，再自动提交一个 commit，message 用当前时间（格式：`YYYY-MM-DD HH:MM`），然后自动 push。
- 只使用 `type`，不使用 `interface`。
- Type-driven development：实现功能前先定义类型，类型确认后再编写实现。
- Immutable data：所有数据以 immutable 方式改动，函数不修改传入的参数，而是返回新对象。修改状态的函数通过返回值体现（如 handler 返回新 Context）。

# Design Principles

从项目 DIARY.md 中提炼的核心设计原则，指导所有设计决策和代码变更。

## 层次 1：思维方式

- **减法优先**：先想象完整方案，再通过实际场景削回最小可用形态。好的抽象吸收结构性重复，代码量应该减少而非膨胀。
- **痛点驱动**：每一轮只解决当下最痛的问题，用新的痛点驱动下一轮。不要一次性设计完美。
- **逃逸优于修补**：当你在别人的抽象层里挣扎时（KDL 编码、shell 环境继承），退后一步，在你能完全控制的层面解决，而非修补适配层。

## 层次 2：类型与数据

- **类型是建模语言**：类型不只是校验工具。先写类型 = 先想清楚数据流和边界，实现只是填空。类型层面的累加（如 `Acc & Out`）让编译器充当状态追踪器。
- **不可变约束业务数据，不约束基础设施**：Context/Registry/Worker 必须不可变。Logger/Memo 等服务可以是 class + readonly —— 构造后冻结即可。区分"数据流"和"服务生命周期"。
- **全量状态查询**：状态检测函数必须报告完整状态（如 `git status --porcelain`），而非某个子集。你无法预知调用者关心什么。

## 层次 3：结构与组合

- **概念局部性**：每个文件/模块只加载一个概念域。重构的目标不是"好看"，而是降低每次阅读的认知负载。
- **代数组合**：组合的结果仍然是同类事物（Capability），可以继续组合。`build()` 封装内部累加细节，对外暴露精确的 `Capability<Init, Acc>` 类型。
- **依赖指向抽象**：模块依赖 Context 接口，不依赖基础设施实现。依赖箭头从"实现细节"转向"抽象接口"。
- **形状匹配选工具**：class 用于"有身份的服务"，闭包用于"创建行为"，plain function 用于无状态变换。匹配概念的形状，不服从教条。

## 层次 4：用户交互

- **自动化 routine，保留 decision**：消除重复操作的摩擦，但不消除有信息量的决策点。confirm + guard 模式是这个原则的实现。

## 层次 5：外部集成

- **外部进程 = 重新理解 context**：把控制权交给 shell/terminal/子进程时，必须验证它的 PATH、cwd、配置继承，不能假设与当前进程一致。
