# LAODA 项目日记

从对话中提炼的 metacognition，记录设计决策背后的思考模式。

---

## 2026-03-11 — 从 CLI 工具到 AI 大脑

### 1. 从复杂到简单的收敛

最初的计划是 LLM 任务分解 → 多子任务多 pane 编排。但很快意识到"一个任务一个 tab 一个 claude"才是真实需求。这体现了一个反复出现的模式：**先想象一个宏大架构，再通过实际使用场景把它削回最小可用形态**。好的设计不是加法，是减法。

### 2. 工具链的隐性摩擦

Zellij 的 `run -i` vs `new-tab -l`、layout KDL 的语法要求、`claude` 命令在子 shell 中找不到 PATH — 这些都不是逻辑错误，而是**工具链的暗礁**。它们消耗了大量调试时间，但本质上是同一类问题：当你把控制权交给外部进程（shell、terminal multiplexer、子进程），你必须重新理解它的 context（PATH、cwd、配置继承）。

### 3. 类型先行是约束也是加速

明确提出 type-driven development 规则。这个决定的深层含义是：**类型是思考的凝固物**。先写类型 = 先想清楚数据流和边界，实现只是填空。重构时定义 `Context`、`Effect`、让 handler 接收 `Intent` 而非散落参数，本质上是把"我想清楚了的东西"固化到编译器能检查的层面。

### 4. Locality 的直觉

对"git 函数不该在 index.ts"、"颜色常量不该散落"的不适感，本质是**认知负载的信号**。当一个文件需要你同时理解 git 操作、ANSI 转义码、readline 状态机和 LLM 调用，你的工作记忆被迫并行加载不相关的概念。重构的真正目标不是"代码好看"，而是**让每次阅读代码时只需加载一个概念域**。

### 5. 渐进式发现需求

对话的需求是逐步浮现的：先是基本 tab 启动 → 然后发现要复制文件夹 → 然后发现要处理 .env.local → 然后发现 monorepo 子包也有 → 然后发现要 install deps → 然后发现要拉最新代码切分支 → 然后发现要 LLM 生成分支名 → 然后发现要意图识别 → 然后发现要缓存意图。每一步都是**上一步使用后的自然追问**。这说明好的开发节奏不是一次性设计完美，而是每一轮只解决当下最痛的问题，然后用新的痛点驱动下一轮。

### 6. Algebraic Effects 的种子

定义了 `Effect` 类型但还没实现 interpreter。这本身就是一个声明：**把"做什么"和"怎么做"分开描述**。即使当前只是类型定义，它已经在说：未来如果要测试 handleTask 的逻辑，不需要真的复制文件夹和调 git，只需要验证它产出了正确的 Effect 序列。这是**可测试性的种子**。

### 7. 底层叙事

整个对话的底层叙事是：**一个人在构建自己的 AI 工作流基础设施**。不是用别人的工具，而是从 terminal multiplexer 层开始，搭建"大脑发出指令 → 多个 AI agent 并行执行"的架构。这个动机决定了所有技术选择 — 为什么是 Zellij 而非 tmux，为什么是文件夹复制而非 git worktree，为什么要 memo 缓存而非每次都调 LLM。每个选择都在优化同一个目标：**降低从想法到执行的摩擦**。
