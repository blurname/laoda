# LAODA 项目日记

从对话中提炼的 metacognition，记录设计决策背后的思考模式。

---

## 2026-03-11 — 从 CLI 工具到 AI 大脑

### 1. 从复杂到简单的收敛

最初的计划是 LLM 任务分解 → 多子任务多 pane 编排。但很快意识到"一个任务一个 tab 一个 claude"才是真实需求。这体现了一个反复出现的模式：**先想象一个宏大架构，再通过实际使用场景把它削回最小可用形态**。好的设计不是加法，是减法。

### 2. 工具链的隐性摩擦

Zellij 的 `run -i` vs `new-tab -l`、layout KDL 的语法要求、`claude` 命令在子 shell 中找不到 PATH — 这些都不是逻辑错误，而是**工具链的暗礁**。它们消耗了大量调试时间，但本质上是同一类问题：当你把控制权交给外部进程（shell、terminal multiplexer、子进程），你必须重新理解它的 context（PATH、cwd、配置继承）。

### 3. 类型先行是约束也是加速

明确提出 type-driven development 规则。这个决定的深层含义是：**类型是思考的凝固物**。先写类型 = 先想清楚数据流和边界，实现只是填空。重构时定义 `Context`、`Effect`、让 handler 接收 `Intent` 而非散落参数，本质上是把"我想清楚了的东西"固化到编译器能检查的层面。

### 4. Locality 的直觉

对"git 函数不该在 index.ts"、"颜色常量不该散落"的不适感，本质是**认知负载的信号**。当一个文件需要你同时理解 git 操作、ANSI 转义码、readline 状态机和 LLM 调用，你的工作记忆被迫并行加载不相关的概念。重构的真正目标不是"代码好看"，而是**让每次阅读代码时只需加载一个概念域**。

### 5. 渐进式发现需求

对话的需求是逐步浮现的：先是基本 tab 启动 → 然后发现要复制文件夹 → 然后发现要处理 .env.local → 然后发现 monorepo 子包也有 → 然后发现要 install deps → 然后发现要拉最新代码切分支 → 然后发现要 LLM 生成分支名 → 然后发现要意图识别 → 然后发现要缓存意图。每一步都是**上一步使用后的自然追问**。这说明好的开发节奏不是一次性设计完美，而是每一轮只解决当下最痛的问题，然后用新的痛点驱动下一轮。

### 6. Algebraic Effects 的种子

定义了 `Effect` 类型但还没实现 interpreter。这本身就是一个声明：**把"做什么"和"怎么做"分开描述**。即使当前只是类型定义，它已经在说：未来如果要测试 handleTask 的逻辑，不需要真的复制文件夹和调 git，只需要验证它产出了正确的 Effect 序列。这是**可测试性的种子**。

### 7. 底层叙事

整个对话的底层叙事是：**一个人在构建自己的 AI 工作流基础设施**。不是用别人的工具，而是从 terminal multiplexer 层开始，搭建"大脑发出指令 → 多个 AI agent 并行执行"的架构。这个动机决定了所有技术选择 — 为什么是 Zellij 而非 tmux，为什么是文件夹复制而非 git worktree，为什么要 memo 缓存而非每次都调 LLM。每个选择都在优化同一个目标：**降低从想法到执行的摩擦**。

---

## 2026-03-14 — 不可变的边界

### 1. "不可变"是关于数据流，不是关于一切

对话中有一个清晰的收敛过程：先是全面审计不可变原则 → 发现 logger/memo 有 module-level `let` → 用闭包替代 → 用 class 替代 → 最后追问"还是不可变吗"。这个追问暴露了一个关键区分：**不可变约束的是业务数据的流动方式（Context/Registry/Worker），不是基础设施的存在方式**。Logger 的 `readonly logDir` 不是"可变状态"，它是构造后冻结的配置。混淆这两者会导致过度设计——把所有东西都变成纯函数反而增加复杂度。原则需要边界，边界需要理解"为什么要这个原则"。

### 2. 闭包 → class 的设计直觉

用户问"用 class 会不会更好"。这不是一个技术问题，而是一个**语义匹配**问题。闭包适合"创建一个行为"，class 适合"创建一个有身份的服务"。Logger 和 Memo 有身份（绑定到某个 project）、有多个方法、可能被扩展——这些都是 class 的信号。最初选闭包是因为"避免 mutable state"的惯性，但 class + readonly 同样满足不可变，且语义更准确。**工具的选择应该匹配概念的形状，而不是服从某个教条。**

### 3. 依赖方向的净化

这次重构的一条暗线是**依赖方向的统一**。之前 handler 直接 import logger 模块 → handler 依赖基础设施细节。改为 Context 注入 `logAction` 后 → handler 只依赖 Context 接口。llm.ts 从 import logger → 接收 `LlmLog` 参数。每一步都在做同一件事：让依赖箭头从"实现细节"转向"抽象接口"。这不是为了 DI 框架，而是为了**每个模块只知道它需要知道的**。

---

## 2026-03-15 — 从点到图的组合直觉

### 1. 逃逸比修补更彻底

KDL 解析中文出错 → 尝试转义 → 放弃，改用 heredoc 写临时脚本。PATH 找不到 claude → 尝试 login shell → 放弃，改用 `which` 预解析绝对路径。两个问题的解法模式相同：**当你在别人的语法/环境里挣扎时，不要修补适配层，直接绕开那个层**。heredoc 绕开了 KDL 字符串编码，绝对路径绕开了 shell 环境继承。这是一种"不在错误的抽象层解决问题"的直觉——退后一步，在你能完全控制的层面解决。

### 2. "我没看到满足这两个条件" — 质疑运行时假设

`isGitClean` 用 `git diff --stat` 只检测已跟踪文件的修改，漏掉 untracked files。用户说"我没看到满足这两个条件啊"，直接否定了代码的运行时行为。这暴露了一个思维盲区：**写代码时的心智模型（"干净 = 没改动"）和 git 的实际语义（"干净 = 没有任何未提交状态"）之间的缝隙**。`git status --porcelain` 是唯一诚实的答案，因为它报告的是 git 眼中的完整状态，而不是某个子集。教训：对"状态查询"类函数，永远用最全面的检测方式，因为你无法预知调用者关心的子集。

### 3. 组合的代数直觉

用户的原始表述："将点（能力）通过线段（胶水代码）连接，组合成图（业务流程）"。这不是工程需求，这是**数学直觉**——他在想范畴论。Capability 是态射，glue 是态射间的适配，FlowBuilder.pipe 是态射组合，build() 是把组合态射变成新的原子态射（嵌套组合）。甚至问了交换律和结合律。这说明用户追求的不是"能跑就行"的流程编排，而是**具有代数性质的组合系统**——组合的结果仍然是同类事物，可以继续组合。理解这个意图才能做出正确的设计决策。

### 4. 类型即累加器

`FlowBuilder<Init, Acc>` 的 `Acc & Out` 交叉类型累加，本质是**让编译器充当流程的状态追踪器**。每个 `.pipe()` 不只是执行步骤，还在类型层面"记账"——流程到这一步为止，积累了哪些数据。这使得 glue 函数的类型签名 `(acc: Acc) => Req` 成为编译时契约：你只能用流程已经产出的东西。用户说"Token 会越来越庞大"时，他识别出了累加器的膨胀问题。解法是让每个 flow 的 `build()` 产出精确的 `Capability<Init, Acc>` 而非泄漏内部累加细节。**类型系统不只是校验工具，它是设计的建模语言。**

### 5. "不是直接走，你给我选项"

用户对 list-pr 流程的反馈——不要自动进入 review，而是给选项。这和 3/11 日记中"降低从想法到执行的摩擦"看似矛盾，实则互补：**自动化应该消除重复操作的摩擦，但不应该消除决策点**。选择"是否 review"是一个有信息量的决策，自动跳过它反而增加了用户的焦虑（"它在帮我做什么？"）。正确的摩擦模型：自动化 routine，保留 decision。confirm + guard 的组合正是这个模型的实现。

### 6. 迁移是减法的证明

从 handler 迁移到 flow：-567 行，+56 行。这个数字本身就是设计质量的信号。当你的新抽象能用十分之一的代码表达同样的行为，说明旧代码中有大量的**结构性重复**被抽象吸收了。但减法的前提是加法——先有 engine.ts 的 ~80 行框架代码，才能让 4 个 flow 各用几行声明。**好的抽象是前期投入，后期提取。**

### 7. 底层叙事：从脚手架到骨骼

3/11 是"搭脚手架"——能跑就行，handler 直接操作 git/zellij/fs。3/14 是"固化边界"——不可变数据、依赖注入、Context 传递。今天是"长出骨骼"——flow engine 是这个系统的骨骼，capability 是关节，glue 是韧带。文件结构的重组（flow/ llm/ infra/）是外部形态开始反映内部结构。项目正在从"一堆功能"变成"一个有机体"。下一步可能是"神经系统"——让 flow 之间能互相感知和协调。
